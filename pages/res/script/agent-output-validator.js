const MAX_ISSUES = 6;
function normalize(value) { return String(value || '').toLocaleLowerCase('it-IT').normalize('NFD').replace(/[\u0300-\u036f]/g, '').replace(/[^a-z0-9\s]/g, ' ').replace(/\s+/g, ' ').trim(); }
function tokens(value) { return new Set(normalize(value).split(' ').filter((token) => token.length > 3)); }
function addIssue(issues, message) { if (issues.length < MAX_ISSUES && !issues.includes(message)) issues.push(message); }
function balancedJavaScript(source, issues) {
  const pairs = { ')': '(', ']': '[', '}': '{' }; const stack = []; let quote = null; let escaped = false; let lineComment = false; let blockComment = false;
  for (let index = 0; index < source.length; index += 1) { const character = source[index]; const next = source[index + 1];
    if (lineComment) { if (character === '\n') lineComment = false; continue; }
    if (blockComment) { if (character === '*' && next === '/') { blockComment = false; index += 1; } continue; }
    if (quote) { if (escaped) { escaped = false; continue; } if (character === '\\') { escaped = true; continue; } if (character === quote) quote = null; continue; }
    if (character === '/' && next === '/') { lineComment = true; index += 1; continue; }
    if (character === '/' && next === '*') { blockComment = true; index += 1; continue; }
    if ('"\'`'.includes(character)) { quote = character; continue; }
    if ('([{'.includes(character)) stack.push(character);
    if (pairs[character] && stack.pop() !== pairs[character]) { addIssue(issues, `parentesi JavaScript non bilanciate vicino al carattere ${index}`); return false; }
  }
  if (quote) addIssue(issues, 'stringa JavaScript non terminata'); if (blockComment) addIssue(issues, 'commento JavaScript non terminato'); if (stack.length) addIssue(issues, 'parentesi JavaScript non bilanciate'); return !quote && !blockComment && !stack.length;
}
function validateHtml(source, issues) { if (!/^\s*(?:<!--|<!doctype html|<html[\s>])/i.test(source)) addIssue(issues, 'documento HTML privo di struttura iniziale'); if (typeof DOMParser !== 'undefined') { const document = new DOMParser().parseFromString(source, 'text/html'); if (!document.querySelector('main, body')) addIssue(issues, 'HTML privo di contenitore body/main'); } [...source.matchAll(/<script(?:\s[^>]*)?>([\s\S]*?)<\/script>/gi)].forEach((match) => balancedJavaScript(match[1], issues)); }
function relevant(source, request, type, issues) { const requestTokens = tokens(request); const outputText = normalize(source); const markers = type === 'html' ? ['html', 'main', 'section', 'script'] : type === 'js' ? ['function', 'const', 'let', 'document', 'fetch', '=>'] : []; if (markers.length && !markers.some((marker) => outputText.includes(marker))) addIssue(issues, `output ${type} privo di costrutti riconoscibili`); if (requestTokens.size >= 3 && requestTokens.size && ![...requestTokens].some((token) => outputText.includes(token))) addIssue(issues, 'output non sufficientemente contestuale alla richiesta'); }
function selectProfile(type, decision = {}) { const category = decision.category || decision.intent || type; if (type === 'html') return { name: 'html-document', confidence: decision.confidence || 0.75 }; if (type === 'js') return { name: ['js', 'html'].includes(category) ? 'javascript-code' : 'javascript-code', confidence: decision.confidence || 0.7 }; if (['weather', 'country', 'wiki'].includes(category)) return { name: 'public-api-response', confidence: decision.confidence || 0.8 }; return { name: 'text-response', confidence: decision.confidence || 0.6 }; }
export function createOutputValidator(state = {}) { return { validate(output, request, type = 'text', decision = {}) { const issues = []; const source = String(output || '').trim(); const profile = selectProfile(type, decision); if (!source) addIssue(issues, 'output vuoto'); if (source.length > 200000) addIssue(issues, 'output oltre il limite di sicurezza'); if (profile.name === 'html-document') validateHtml(source, issues); if (profile.name === 'javascript-code') balancedJavaScript(source, issues); if (profile.name === 'html-document' && !/<(?:html|main|section|form|article|body)[\s>]/i.test(source)) addIssue(issues, 'output HTML privo di elementi validi'); if (profile.name === 'javascript-code' && /<!doctype|<html[\s>]/i.test(source)) addIssue(issues, 'output JavaScript contiene una pagina HTML'); relevant(source, request, type, issues); const score = Math.max(0, 1 - issues.length * 0.18); return { accepted: issues.length === 0, score, issues, type, request, profile, contextTurns: state.context?.turns?.length || 0 }; }, rejectMessage(result) { return `Output scartato dal secondo agente (${result.profile.name}, ${(result.score * 100).toFixed(0)}%):\n- ${result.issues.join('\n- ')}\nRiformula la richiesta o specifica meglio il formato desiderato.`; } }; }